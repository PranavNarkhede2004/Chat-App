services:
  - type: web
    name: chat-app
    runtime: node
    buildCommand: npm ci && npm ci --prefix backend && npm ci --prefix frontend && npm run build --prefix frontend
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
